//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by Fernflower decompiler)
//

package homeservicessystem;

import java.util.ArrayList;
import java.util.List;

public class ServiceProvider extends User {
    private String specialty;
    private double rating;
    private int totalRatings;
    private boolean isAvailable;
    private List<Booking> assignedBookings;
    private List<Service> offeredServices;

    public ServiceProvider(String userId, String name, String email, String password, String phone, String specialty) {
        super(userId, name, email, password, phone);
        this.specialty = specialty;
        this.rating = (double)0.0F;
        this.totalRatings = 0;
        this.isAvailable = true;
        this.assignedBookings = new ArrayList();
        this.offeredServices = new ArrayList();
    }

    public String getUserType() {
        return "ServiceProvider";
    }

    public String getSpecialty() {
        return this.specialty;
    }

    public void setSpecialty(String specialty) {
        this.specialty = specialty;
    }

    public double getRating() {
        return this.rating;
    }

    public int getTotalRatings() {
        return this.totalRatings;
    }

    public boolean isAvailable() {
        return this.isAvailable;
    }

    public void setAvailable(boolean available) {
        this.isAvailable = available;
    }

    public List<Booking> getAssignedBookings() {
        return this.assignedBookings;
    }

    public List<Service> getOfferedServices() {
        return this.offeredServices;
    }

    public void addService(Service service) {
        this.offeredServices.add(service);
    }

    public void addBooking(Booking booking) {
        this.assignedBookings.add(booking);
    }

    public void addRating(int stars) {
        ++this.totalRatings;
        this.rating = (this.rating * (double)(this.totalRatings - 1) + (double)stars) / (double)this.totalRatings;
    }

    public void restoreRating(double savedRating, int savedTotal) {
        this.rating = savedRating;
        this.totalRatings = savedTotal;
    }

    public String getFormattedRating() {
        return this.totalRatings == 0 ? "No ratings yet" : String.format("%.1f ★ (%d reviews)", this.rating, this.totalRatings);
    }

    public List<Booking> getPendingBookings() {
        List<Booking> pending = new ArrayList();

        for(Booking b : this.assignedBookings) {
            if (b.getStatus().equals("Pending")) {
                pending.add(b);
            }
        }

        return pending;
    }
}
